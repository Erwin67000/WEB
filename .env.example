# Variables Vite (front) — optionnel (Checkout redirect n’en a pas besoin)
# Clés : https://dashboard.stripe.com/apikeys
# La clé secrète Stripe (STRIPE_SECRET_KEY) ne doit JAMAIS être ici — uniquement .dev.vars / wrangler secrets.

# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx

