# Cloudflare Worker project name (must match dashboard: philaedesign2)
name = "philaedesign2"
compatibility_date = "2025-03-01"
workers_dev = true

# Vite production output — same UI as localhost:3102
[assets]
directory = "./dist"
# React Router SPA: /boutique, /configurateur, …
not_found_handling = "single-page-application"
html_handling = "auto-trailing-slash"
